Frontend-->React(Facebook),angular(google)

1. CRUD operation
c-data is created and sent to Database
r-read the data from the Database
u-update the data 
d-delete the data 

2. Fetch the data to the Frontend using the Backend APIs

Backend-->

Database-->


HTML:

Tags
Attributes
Values

<>--opening Tag
</>--closing tag
<pre>

WEB APP CONTENTS:
Header part

Body part

Footer part

relative 


span--inline container to group element for styling.
=>take the only the size of the content.

div--block line container  to group element for styling.
=>take the entire width of the viewport





CSS:

comment line /**/

way to declare

=>inline
    <button style="font-size: 30px; background-color: black; color: white;border-radius: 5px;">click me</button>


=>internal(inside the Head tag)
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
        }

        header {
            background-color: burlywood;
            padding: 10px;
        }

        main {
            padding: 20px;
        }

        footer {
            background-color: lightgray;
            padding: 10px;
            text-align: center;
            position: fixed;
            bottom: 0;
            width: 100%;
        }
    </style>

=>external(file.css)
    inside the Head tag
    By using link tag
    <link rel="stylesheet" href="style.css">

    id is accessed through #(hashtag)
    class is accessed through .(dot)



    color:
    =>name
    =>RGB
    =>hexa
    =>hsl values


    Shadow:
    1st-horizontal
    2nd-vertical
    3rd-blur

    + => right
    - => left


    Margin:


    float-> allow elements to flow around it

    Overflow: property that set the desired behaviour when the content not fit in the parent element/box(overflow)
        =>visible
        =>hidden
        =>scroll
        =>auto
    

    Display:=>property specify an if/how an element is displayed
        => block-level--> start with a new line/take the full width of the available viewport(h1,div,form,header,footer)

        => inline-level--> do not starts from the new line/width is limited to what is needed based on the content.(spam,a,img)



   position:-> control how element  is placed in a webpage.
        =>relative - you    

   combinators:-> Explain the relationship between the listed slector
        space-" " = decesdent
        > = Child
        ~ = general sibling
        + = adjacent sibiling